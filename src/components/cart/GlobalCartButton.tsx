"use client";

// GlobalCartButton has been retired.
// The cart icon now lives inside the Navbar (NavbarCartButton),
// which renders on every page as part of the sticky header.
//
// This file is kept to avoid broken imports during the transition.
// It can be safely deleted once all import references are removed.
export const GlobalCartButton = () => null;
